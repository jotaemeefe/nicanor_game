# OpenCode Instructions Adapter

Project-specific durable instructions should live in the shared root `AGENTS.md`.

Use this folder only for OpenCode-specific notes that should not pollute the shared harness-neutral instructions.
