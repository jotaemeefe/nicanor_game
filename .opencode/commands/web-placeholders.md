# /web-placeholders

Resolve to the shared `commands/web-placeholders.md` workflow.

## Adapter behavior
- primary owners: `web-reviewer`, `architect`
- follow the shared command instructions for invoked agents, required skills, and expected output
