# /context

Resolve to the shared `commands/context.md` workflow.

## Adapter behavior
- primary owners: `producer`, `planner`
- follow the shared command instructions for invoked agents, required skills, and expected output
