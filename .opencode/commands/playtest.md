# /playtest

Resolve to the shared `commands/playtest.md` workflow.

## Adapter behavior
- primary owners: `qa-lead`, `playtest-analyst`
- follow the shared command instructions for invoked agents, required skills, and expected output
