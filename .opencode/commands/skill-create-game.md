# /skill-create-game

Resolve to the shared `commands/skill-create-game.md` workflow.

## Adapter behavior
- primary owners: `doc-updater`, `planner`
- follow the shared command instructions for invoked agents, required skills, and expected output
