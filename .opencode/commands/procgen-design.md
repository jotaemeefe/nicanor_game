# /procgen-design

Resolve to the shared `commands/procgen-design.md` workflow.

## Adapter behavior
- primary owners: `systems-designer`, `level-designer`, `gameplay-programmer`
- follow the shared command instructions for invoked agents, required skills, and expected output
