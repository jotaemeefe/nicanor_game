# /qa-plan

Resolve to the shared `commands/qa-plan.md` workflow.

## Adapter behavior
- primary owners: `qa-lead`
- follow the shared command instructions for invoked agents, required skills, and expected output
