# /playtest-report

Resolve to the shared `commands/playtest-report.md` workflow.

## Adapter behavior
- primary owners: `playtest-analyst`, `producer`
- follow the shared command instructions for invoked agents, required skills, and expected output
