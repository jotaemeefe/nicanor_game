# Godot 4+ specific ignores
.godot/
*.translation
export.cfg
# export_presets.cfg IS tracked here on purpose: the Web preset carries no
# credentials and documents how the published demo is built.

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores (unused in this project, kept for safety)
.mono/
data_*/
