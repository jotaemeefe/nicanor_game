{
  "$schema": "../schemas/mcp-servers.schema.json",
  "version": 1,
  "description": "Recommended MCP server registry for the game-development scaffold. This file is intentionally configuration-oriented and uses placeholder commands or environment keys where local setup differs by team.",
  "profiles": {
    "baseline": [
      "filesystem",
      "git",
      "github",
      "ripgrep",
      "fetch-docs"
    ],
    "unity": [
      "filesystem",
      "git",
      "github",
      "ripgrep",
      "fetch-docs",
      "unity-docs",
      "unity-package-registry",
      "issue-tracker",
      "blender",
      "unity-editor"
    ],
    "unreal": [
      "filesystem",
      "git",
      "github",
      "ripgrep",
      "fetch-docs",
      "unreal-docs",
      "issue-tracker",
      "blender",
      "unreal-editor"
    ],
    "godot": [
      "filesystem",
      "git",
      "github",
      "ripgrep",
      "fetch-docs",
      "godot-docs",
      "issue-tracker",
      "blender",
      "godot-editor"
    ],
    "web": [
      "filesystem",
      "git",
      "github",
      "ripgrep",
      "fetch-docs",
      "issue-tracker",
      "playwright",
      "blender"
    ],
    "liveops": [
      "filesystem",
      "git",
      "github",
      "ripgrep",
      "fetch-docs",
      "issue-tracker",
      "telemetry",
      "storefront-docs"
    ],
    "release": [
      "filesystem",
      "git",
      "github",
      "ripgrep",
      "fetch-docs",
      "issue-tracker",
      "storefront-docs",
      "ci-builds",
      "playwright"
    ]
  },
  "servers": {
    "filesystem": {
      "category": "local",
      "enabled_by_default": true,
      "recommended": true,
      "purpose": "Read and traverse the working repository, generated docs, logs, and local output files.",
      "when_to_use": [
        "inspect project structure",
        "read generated design or technical docs",
        "review logs or exported reports"
      ],
      "transport": "stdio",
      "command": "<your-filesystem-mcp-server-command>",
      "args": [
        "<project-root>"
      ],
      "env": {},
      "notes": [
        "Restrict access to the project root or approved workspace directories.",
        "Prefer read-first workflows; write access should be intentional."
      ]
    },
    "git": {
      "category": "source-control",
      "enabled_by_default": true,
      "recommended": true,
      "purpose": "Inspect branches, diffs, blame, recent changes, and release history.",
      "when_to_use": [
        "review change history",
        "trace regressions",
        "prepare release notes or patch notes"
      ],
      "transport": "stdio",
      "command": "<your-git-mcp-server-command>",
      "args": [],
      "env": {},
      "notes": [
        "Keep repository scope explicit.",
        "Use together with filesystem access for code and content reviews."
      ]
    },
    "github": {
      "category": "source-control",
      "enabled_by_default": true,
      "recommended": true,
      "purpose": "Review pull requests, issues, discussions, and remote repository state.",
      "when_to_use": [
        "read PR discussions",
        "triage issues",
        "inspect CI status or release metadata"
      ],
      "transport": "stdio",
      "command": "<your-github-mcp-server-command>",
      "args": [],
      "env": {
        "GITHUB_TOKEN": "<required>"
      },
      "notes": [
        "Use least-privilege tokens.",
        "Useful for PR review, release planning, and issue triage agents."
      ]
    },
    "ripgrep": {
      "category": "search",
      "enabled_by_default": true,
      "recommended": true,
      "purpose": "Fast codebase and content text search for symbols, config keys, and repeated patterns.",
      "when_to_use": [
        "trace feature wiring",
        "find references across large projects",
        "audit naming or migration impact"
      ],
      "transport": "stdio",
      "command": "<your-search-mcp-server-command>",
      "args": [
        "<project-root>"
      ],
      "env": {},
      "notes": [
        "Best paired with filesystem and git servers.",
        "Critical for large Unity, Unreal, and Godot repositories."
      ]
    },
    "fetch-docs": {
      "category": "documentation",
      "enabled_by_default": true,
      "recommended": true,
      "purpose": "Retrieve official documentation pages, package docs, API references, and public technical references.",
      "when_to_use": [
        "verify engine APIs",
        "check package docs",
        "confirm release or platform requirements"
      ],
      "transport": "stdio",
      "command": "<your-web-docs-mcp-server-command>",
      "args": [],
      "env": {},
      "notes": [
        "Prefer official docs domains in production workflows.",
        "Useful for all engine-specific reviewers and build resolvers."
      ]
    },
    "unity-docs": {
      "category": "engine-docs",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Unity scripting, manual, package, and platform-build documentation.",
      "when_to_use": [
        "confirm Unity API usage",
        "review package behavior",
        "debug build or import workflows"
      ],
      "transport": "stdio",
      "command": "<your-unity-docs-mcp-server-command>",
      "args": [],
      "env": {},
      "notes": [
        "Use for Unity-only sessions and profiles.",
        "Keep package-version context explicit in docs and technical design."
      ]
    },
    "unity-package-registry": {
      "category": "engine-docs",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Lookup Unity package metadata, versions, changelogs, and dependency notes.",
      "when_to_use": [
        "review package upgrade impact",
        "compare package versions",
        "investigate package dependencies"
      ],
      "transport": "stdio",
      "command": "<your-unity-package-mcp-server-command>",
      "args": [],
      "env": {},
      "notes": [
        "Useful for build engineers, reviewers, and package governance workflows."
      ]
    },
    "unreal-docs": {
      "category": "engine-docs",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Unreal documentation for framework classes, packaging, rendering, networking, and editor workflows.",
      "when_to_use": [
        "confirm Unreal framework usage",
        "review packaging requirements",
        "check rendering or networking constraints"
      ],
      "transport": "stdio",
      "command": "<your-unreal-docs-mcp-server-command>",
      "args": [],
      "env": {},
      "notes": [
        "Use for Unreal-only sessions and profile installs."
      ]
    },
    "godot-docs": {
      "category": "engine-docs",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Godot class reference, export docs, scene/node docs, and editor workflow guidance.",
      "when_to_use": [
        "confirm node or signal usage",
        "review export settings",
        "check scene architecture patterns"
      ],
      "transport": "stdio",
      "command": "<your-godot-docs-mcp-server-command>",
      "args": [],
      "env": {},
      "notes": [
        "Use for Godot-only sessions and profile installs."
      ]
    },
    "issue-tracker": {
      "category": "production",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Read and update backlog items, bugs, milestones, and release tasks in the team issue tracker.",
      "when_to_use": [
        "triage bugs",
        "track milestone scope",
        "audit release readiness"
      ],
      "transport": "stdio",
      "command": "<your-issue-tracker-mcp-server-command>",
      "args": [],
      "env": {
        "ISSUE_TRACKER_TOKEN": "<required-if-needed>",
        "ISSUE_TRACKER_BASE_URL": "<required-if-needed>"
      },
      "notes": [
        "Recommended for producer, QA, release, and doc-updater workflows.",
        "Keep write permissions intentional."
      ]
    },
    "telemetry": {
      "category": "analytics",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Inspect dashboards, KPI tables, event definitions, and post-launch metrics.",
      "when_to_use": [
        "review retention or progression metrics",
        "audit telemetry coverage",
        "analyze liveops results"
      ],
      "transport": "stdio",
      "command": "<your-telemetry-mcp-server-command>",
      "args": [],
      "env": {
        "ANALYTICS_TOKEN": "<required-if-needed>",
        "ANALYTICS_PROJECT": "<required-if-needed>"
      },
      "notes": [
        "Use for telemetry analysts, liveops managers, and mobile F2P reviews.",
        "Avoid exposing unnecessary player-level data."
      ]
    },
    "fal-media": {
      "category": "asset-generation",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Interactive AI asset generation via the fal.ai model catalog: images, skyboxes, 3D models, sound effects, music, speech, and video.",
      "when_to_use": [
        "explore or compare generation models before committing them to the provider registry",
        "iterate interactively on prompts before a reproducible generation run",
        "estimate generation cost before batch work"
      ],
      "transport": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "fal-ai-mcp-server"
      ],
      "env": {
        "FAL_KEY": "<required>"
      },
      "notes": [
        "Optional convenience layer. Reproducible production generation goes through scripts/generate-assets.js and manifests/asset-providers.json so provenance sidecars are written.",
        "Follow the AI-generated asset rules in rules/common/asset-pipeline.md regardless of which path generated the file."
      ]
    },
    "blender": {
      "category": "dcc-tool",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Drive a running Blender instance to inspect scenes and run bpy Python for 3D asset work: build/normalize geometry, set scale/axis/pivot, assign materials, pull PolyHaven/Sketchfab assets, validate against import rules, and export GLB/FBX.",
      "when_to_use": [
        "normalize a generated or downloaded model to the project's unit/axis/pivot and naming conventions before it reaches a scene",
        "run the 3d-asset-pipeline import checklist (scale probe, orientation, pivot, polycount) and report pass/fail",
        "kitbash modular pieces with snapping pivots, or pull HDRIs/textures from PolyHaven",
        "clean up and re-export existing rigs/animations (rename actions, trim ranges, fix framerate, assisted retarget)"
      ],
      "transport": "stdio",
      "command": "uvx",
      "args": [
        "blender-mcp"
      ],
      "env": {},
      "notes": [
        "DCC tool, engine-neutral: it produces GLB/FBX that each engine layer imports under its own rules, so it does not break engine isolation.",
        "Interactive only — needs Blender open with the BlenderMCP addon running (not headless); like fal-media and playwright it is an iteration layer, not a CI/deterministic path.",
        "Scope: geometry, normalization, import validation, and export are solid. Rigging and character animation are only assisted (auto-rig, retarget, clip cleanup) — precise bone placement, weight painting, and keyframe timing still need a human. Do not present it as an autonomous rigger/animator.",
        "Pairs with skills 3d-asset-pipeline, rigging-skinning-pipeline, 3d-animation-pipeline, lighting-lod-pipeline and the /art-3d-pass command; follow rules/common/asset-pipeline.md for any AI-generated source."
      ]
    },
    "unity-editor": {
      "category": "engine-control",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Drive the Unity Editor from the assistant via MCP for Unity (CoplayDev): inspect and modify scenes and GameObjects, manage and import assets, run Editor menu items, read the console, and edit/run C# — so scene scaffolding and audits act directly in the Editor instead of hand-editing YAML.",
      "when_to_use": [
        "scaffold or rewire a scene live in the Editor (create GameObjects, set components/references) instead of editing .unity/.prefab YAML by hand",
        "audit scene/prefab structure and serialized references against the unity rules while the project is open",
        "run Editor menu items, trigger imports, or read the console while diagnosing a build or import",
        "apply a unity-setup/scene-bootstrap result directly to the open project"
      ],
      "transport": "stdio",
      "command": "uvx",
      "args": [
        "--from",
        "mcpforunityserver",
        "mcp-for-unity",
        "--transport",
        "stdio"
      ],
      "env": {},
      "notes": [
        "Two halves like the Blender MCP: install the Unity-side bridge in your project (Package Manager -> Add package from git URL: https://github.com/CoplayDev/unity-mcp.git?path=/MCPForUnity#main — pin a release tag like #v10.0.0 for reproducible installs; also available via OpenUPM as com.coplaydev.unity-mcp) and keep the Unity Editor open; the harness launches this server (PyPI package mcpforunityserver) which connects to it.",
        "v10 (2026-06) added Tool Groups (tools organized by domain: vfx, animation, UI, testing) and improved multi-instance routing; when upgrading a project from v9 or older, follow the project's migration guide.",
        "Prereqs: Unity 2021.3 LTS+, Python 3.10+ with uv (provides uvx). First run downloads mcpforunityserver from PyPI.",
        "Engine-specific (engine-control): use only inside Unity work; it does not apply to Unreal/Godot/web. Keep Unity implementation detail in the Unity rules layer.",
        "Pairs with the unity-setup, scene-bootstrap, unity-scene-audit, and unity-review commands. Distinct from unity-docs / unity-package-registry, which are documentation servers, not Editor control."
      ]
    },
    "unity-editor-native": {
      "category": "engine-control",
      "enabled_by_default": false,
      "recommended": false,
      "purpose": "Alternative Unity Editor control via Unity's built-in MCP support (Unity 6+, com.unity.ai.assistant): the Editor runs its own MCP bridge and clients connect through a relay binary — first-party, no Python/uv, with in-Editor connection approval.",
      "when_to_use": [
        "drive the Unity Editor with zero external dependencies on Unity 6+ (no Python/uv, no third-party bridge package)",
        "work under Unity's built-in client-approval flow (Project Settings -> AI -> Unity MCP) when team policy prefers first-party tooling"
      ],
      "transport": "stdio",
      "command": "<your-home-dir>/.unity/relay/relay_win.exe",
      "args": [
        "--mcp"
      ],
      "env": {},
      "notes": [
        "NOT pre-wired: the relay binary Unity installs lives under the user's home directory and is per-platform (Windows `%USERPROFILE%\\.unity\\relay\\relay_win.exe`, macOS arm64 `~/.unity/relay/relay_mac_arm64.app/Contents/MacOS/relay_mac_arm64`, macOS x64 `~/.unity/relay/relay_mac_x64.app/Contents/MacOS/relay_mac_x64`, Linux `~/.unity/relay/relay_linux`), so the command is a per-machine placeholder — register it in your own client config with the real absolute path, always passing `--mcp`.",
        "Requires Unity 6 (6000.0)+ with the com.unity.ai.assistant package installed; the bridge starts with the Editor (check Project Settings -> AI -> Unity MCP shows Running). New clients appear there as a Pending Connection you must Accept once.",
        "The pre-wired `unity-editor` (CoplayDev bridge) remains the default: portable command, Unity 2021.3+, and a much larger tool catalog. Choose this native server for first-party-only setups on Unity 6+; do not run both against the same Editor session.",
        "Engine-specific (engine-control): use only inside Unity work; it does not apply to Unreal/Godot/web. Keep Unity implementation detail in the Unity rules layer."
      ]
    },
    "godot-editor": {
      "category": "engine-control",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Drive the Godot engine via the Godot MCP server (Coding-Solo): launch the editor, run projects, read/modify scenes and nodes, edit scripts, and capture debug/console output so scene work and run-time debugging act on the real project.",
      "when_to_use": [
        "run the project and capture debug output / errors while diagnosing a bug",
        "inspect or modify scene/node structure against the godot rules instead of editing .tscn by hand",
        "apply a godot-setup / scene work result to the open project",
        "launch the editor or a scene for a quick playtest loop"
      ],
      "transport": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@coding-solo/godot-mcp"
      ],
      "env": {},
      "notes": [
        "Portable: runs straight from npm (@coding-solo/godot-mcp) via npx, no clone or path needed. GODOT_PATH is optional (auto-detected); set it in your environment only if detection fails.",
        "Godot 4.x only — like the rest of the Godot MCP ecosystem it does not support 3.x projects. There is no first-party Godot MCP server; heavier community alternatives exist as in-editor addons (e.g. Godot MCP Native, Godot MCP Pro on the Asset Library) if you need a deeper toolset than this portable server, at the cost of a per-project addon install.",
        "Engine-specific (engine-control): use only inside Godot work; it does not apply to Unity/Unreal/web. Keep Godot implementation detail in the Godot rules layer.",
        "Pairs with the godot-setup, godot-scene-audit, godot-build-fix, and godot-review commands. Distinct from godot-docs, which is a documentation server, not engine control."
      ]
    },
    "unreal-editor": {
      "category": "engine-control",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Drive the Unreal Editor via Epic's official Unreal MCP plugin (UE 5.8+, Experimental): the editor embeds an MCP server over local HTTP exposing actor spawning, lighting, material instances, Slate widget inspection, and automation tests as tools.",
      "when_to_use": [
        "spawn/edit actors and configure lighting or materials live instead of by hand",
        "audit level structure against the unreal rules while the project is open",
        "run automation tests or editor operations while diagnosing a build",
        "apply an unreal-setup result to the open project"
      ],
      "transport": "http",
      "url": "http://127.0.0.1:8000/mcp",
      "env": {},
      "notes": [
        "Official first-party plugin, pre-wired and portable: no clone, no Python, no machine paths. In your Unreal project enable Edit -> Plugins -> 'Unreal MCP' (its Toolset Registry dependency enables automatically), restart, and keep the Editor open; the server listens on http://127.0.0.1:8000/mcp (port/path configurable under Editor Preferences -> Model Context Protocol).",
        "Experimental in UE 5.8 and deliberately minimal; tool discovery runs through meta-tools (list_toolsets / describe_toolset / call_tool). Local-loopback only, no auth layer — never expose the port beyond the machine. The editor can also emit client configs itself via the console command `ModelContextProtocol.GenerateClientConfig <Client>`, but the scaffold's committed configs already cover that.",
        "Needs UE 5.8+. On older engine versions, or if you need a broader toolset than the experimental plugin ships, use the community alternative `unreal-editor-community` below.",
        "Engine-specific (engine-control): use only inside Unreal work; it does not apply to Unity/Godot/web. Keep Unreal implementation detail in the Unreal rules layer.",
        "Pairs with the unreal-setup, unreal-blueprint-audit, unreal-build-fix, and unreal-review commands. Distinct from unreal-docs, which is a documentation server, not Editor control."
      ]
    },
    "unreal-editor-community": {
      "category": "engine-control",
      "enabled_by_default": false,
      "recommended": false,
      "purpose": "Alternative Unreal Editor control via the community Unreal MCP server (chongdashu): actors, blueprints, levels, and editor automation — a broader toolset than Epic's experimental plugin, for UE versions before 5.8 or deeper blueprint work.",
      "when_to_use": [
        "drive the Unreal Editor on engine versions older than UE 5.8 (no official MCP plugin)",
        "wire blueprints live — blueprint authoring is beyond the official plugin's experimental toolset"
      ],
      "transport": "stdio",
      "command": "uv",
      "args": [
        "--directory",
        "<path-to-your-clone>/unreal-mcp/Python",
        "run",
        "unreal_mcp_server.py"
      ],
      "env": {},
      "notes": [
        "NOT pre-wired: the community server (github.com/chongdashu/unreal-mcp) has no portable npx/uvx package — it launches from a local clone via `uv --directory <path>/Python run unreal_mcp_server.py`, so the path is a per-machine placeholder. Replace it in your own client config; the scaffold never commits a machine path.",
        "Two halves like the other engine servers: install the UnrealMCP plugin (MCPGameProject/Plugins/UnrealMCP) in your project and keep the Editor open; this server connects to it. Prereqs: Python 3.10+ with uv.",
        "Prefer the official `unreal-editor` entry on UE 5.8+ — it is first-party, pre-wired, and needs no local clone. Do not run both against the same Editor session.",
        "Engine-specific (engine-control): use only inside Unreal work; it does not apply to Unity/Godot/web. Keep Unreal implementation detail in the Unreal rules layer."
      ]
    },
    "playwright": {
      "category": "playtest",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Drive a real browser to playtest and QA web (HTML5) builds: navigate, click/drag/type, capture screenshots, and read console + page errors.",
      "when_to_use": [
        "smoke-test a running web build end to end (boot, start, play, restart)",
        "capture gameplay or menu screenshots for review",
        "verify menu/HUD flows and surface console or page errors",
        "check GPU-memory-flat or perf signals exposed on window during play"
      ],
      "transport": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest"
      ],
      "env": {},
      "notes": [
        "Microsoft's official Playwright MCP. First run needs browsers installed: `npx playwright install chromium`.",
        "Point it at a local dev/preview server (e.g. `npm run dev` / `npm run preview`), not a file:// path, so ES modules load.",
        "Optional interactive layer. Each sample still keeps a reproducible smoke path in its own scripts/ (headless sim test + a committed Playwright script); the MCP is for live, ad hoc QA.",
        "Web-target only — it tests browser builds, not the Unity/Unreal/Godot editors (use an engine MCP for those)."
      ]
    },
    "storefront-docs": {
      "category": "release",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Review store submission rules, metadata requirements, release checklists, and platform documentation.",
      "when_to_use": [
        "prepare release submissions",
        "verify metadata requirements",
        "check platform release constraints"
      ],
      "transport": "stdio",
      "command": "<your-storefront-docs-mcp-server-command>",
      "args": [],
      "env": {},
      "notes": [
        "Useful for release-manager and console-compliance-reviewer workflows."
      ]
    },
    "ci-builds": {
      "category": "release",
      "enabled_by_default": false,
      "recommended": true,
      "purpose": "Inspect CI pipelines, build artifacts, test outputs, and packaging logs.",
      "when_to_use": [
        "investigate build failures",
        "review test output",
        "audit release candidates"
      ],
      "transport": "stdio",
      "command": "<your-ci-mcp-server-command>",
      "args": [],
      "env": {
        "CI_TOKEN": "<required-if-needed>",
        "CI_BASE_URL": "<required-if-needed>"
      },
      "notes": [
        "Recommended for build engineers, release managers, and engine build resolvers."
      ]
    }
  }
}
